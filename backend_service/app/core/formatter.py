# # This file formats extracted legal information into a readable string.

# # Format extracted results into a readable string
# def format_results(results):
#     lines = []
#     for row in results:
#         lines.append(f"{row['date']} - {row['event']} - {row['obligation']}")
#     return "\n".join(lines)
